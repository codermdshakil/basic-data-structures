#include <bits/stdc++.h>
using namespace std;

// create double linked list Node using prev, val, next
class Node
{
public:
    Node *prev;
    int val;
    Node *next;
    Node(int val)
    {
        this->prev = NULL;
        this->val = val;
        this->next = NULL;
    }
};


int main(){
    
    return 0;
}